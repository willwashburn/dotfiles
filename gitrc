[alias]
        b = branch
        ba = branch -a
        ci = commit
        co = checkout
        d = diff
        dc = diff cached
        fp = format-patch

        fixup = commit --amend --resuse-message HEAD
